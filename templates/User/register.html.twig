{% extends 'layout.html.twig' %}
{% block title %}Inscrire un utilisateur{% endblock %}
{% block main %}
    <h1 class="text-center">Inscrire un utilisateur</h1>
    <div>
        {{ form_start(userForm) }}

            <div>
                {{ form_label(userForm.pseudo) }}
                {{ form_widget(userForm.pseudo) }}
                {{ form_errors(userForm.pseudo) }}
            </div>

            <div>
                    {{ form_label(userForm.prenom) }}
                    {{ form_widget(userForm.prenom) }}
                    {{ form_errors(userForm.prenom) }}
            </div>

            <div>
                    {{ form_label(userForm.nom) }}
                    {{ form_widget(userForm.nom) }}
                    {{ form_errors(userForm.nom) }}
            </div>

            <div>
                    {{ form_label(userForm.telephone) }}
                    {{ form_widget(userForm.telephone) }}
                    {{ form_errors(userForm.telephone) }}
            </div>

            <div>
                    {{ form_label(userForm.mail) }}
                    {{ form_widget(userForm.mail) }}
                    {{ form_errors(userForm.mail) }}
            </div>

            <div>
                    {{ form_widget(userForm.password) }}
                    {{ form_errors(userForm.password) }}
            </div>

            <div>
                    {{ form_label(userForm.campus) }}
                    {{ form_widget(userForm.campus) }}
                    {{ form_errors(userForm.campus) }}
            </div>

            <div>
                {{ form_label(userForm.administrateur) }}
                {{ form_widget(userForm.administrateur) }}
                {{ form_errors(userForm.administrateur) }}
            </div>

            {{ form_row(userForm._token) }}
            <button class="btn btn-primary" type="submit">Inscrire un nouvel utilisateur</button>
        {{ form_end(userForm, {'render_rest': false}) }}

    </div>
    <br>
    <a class="btn btn-light" href="javascript:history.back()">Retour</a>
{% endblock %}